{
  "buildCommand": "npm --prefix packages/shared install && npm --prefix packages/backend install && npm --prefix packages/frontend install && npm run build",
  "outputDirectory": "packages/frontend/dist",
  "env": {
    "NODE_ENV": "production"
  },
  "functions": {
    "api/index.ts": {
      "memory": 3008,
      "maxDuration": 40
    }
  }
}
