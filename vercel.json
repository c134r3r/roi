{
  "buildCommand": "npm run install:all && npm run build",
  "outputDirectory": "packages/frontend/dist",
  "env": {
    "NODE_ENV": "production",
    "USE_POSTGRES": "true"
  },
  "functions": {
    "api/index.ts": {
      "memory": 3008,
      "maxDuration": 30
    }
  }
}
